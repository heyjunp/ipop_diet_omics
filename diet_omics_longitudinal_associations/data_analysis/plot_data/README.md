# Public Plot Data

This folder stores public plotting tables derived from the shareable files already kept in `data_analysis/`.

Rules:

- only final plotting tables are stored here
- all files are plain `csv`
- no raw omics matrices or private intermediate objects should be added here

The plotting scripts in `code/` read from this folder only.
