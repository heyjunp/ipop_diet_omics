# Public Plotting Code

This `code/` folder only contains plotting scripts that start from public plotting data in `data_analysis/plot_data/`.

Use `1_code/` for the original full workflow.

Use `code/` when you want to:

- regenerate figures from already prepared public plotting tables
- avoid touching raw data or private intermediate objects
- share plotting-only code with collaborators
